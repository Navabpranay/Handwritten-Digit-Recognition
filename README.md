# 🧠 Handwritten Digit Recognition using CNN (MNIST)

This project implements a **Handwritten Digit Recognition System** using the well-known **MNIST dataset**.  
A **Convolutional Neural Network (CNN)** is used to classify digits (0–9) from 28×28 grayscale images with **98–99% accuracy**, making it suitable for OCR applications, document scanning, postal automation, and more.

---

## 📂 Dataset Overview

The model uses the **MNIST dataset**, consisting of:

- **70,000 images**
- **28×28 grayscale**
- **60,000 training samples**
- **10,000 test samples**
- 10 digit classes (0–9)

Each image contains a single handwritten digit centered on a black background.

---

## ✅ Project Workflow

### **1. Data Loading**
- Loaded MNIST using Keras (`mnist.load_data()`).
- Automatically split into **training** and **testing** sets.

---

### **2. Data Preprocessing**
- Reshaped data into CNN-compatible format:
- Normalized pixel values from **0–255 to 0–1** for faster convergence.
- One-hot encoded the labels.
- Visualized sample images for dataset understanding.

---

### **3. Model Building**

Two models were built:

#### ✅ Fully Connected Neural Network (Baseline)
- Flattened images  
- Passed through dense layers  
- Reasonable performance, but not ideal for image recognition  

#### ✅ Convolutional Neural Network (Final Model)
- Used:
- `Conv2D` layers
- `MaxPooling2D`
- Dropout for regularization
- Dense Softmax output layer  
- CNN achieved superior accuracy due to spatial feature extraction.

---

### **4. Model Compilation**
- **Loss Function:** Categorical Crossentropy  
- **Optimizer:** Adam  
- **Metric:** Accuracy  

---

### **5. Model Training**
- Trained for **10 epochs**
- Used validation split for generalization monitoring
- Observed steady accuracy improvement across epochs

---

### **6. Model Evaluation & Saving**
- Evaluated on the **10,000-image test set**
- Achieved **98–99% accuracy**
- Saved model using:

---

### **7. Prediction on New Images**
To make predictions on external images:

- Resize to **28×28**
- Convert to **grayscale**
- Normalize pixel values
- Reshape to `(1, 28, 28, 1)`
- Load the saved model for prediction
- Handled inverted color backgrounds when needed

This ensures accurate predictions on real-world handwritten digits.

---

## 📊 Results

- ✅ **High accuracy: 98–99%**
- ✅ Strong performance on both MNIST and external test images  
- ✅ CNN model significantly outperformed the dense network  

(Add graphs, confusion matrix, or prediction images here if available)

---

## 🛠️ Technologies Used

- **Python**
- **Deep Learning (CNN)**
- **TensorFlow & Keras**
- **NumPy**
- **OpenCV**
- **Matplotlib**
- **MNIST Dataset**
- **Visual Studio Code (VS Code)**

---

## ▶️ How to Run This Project

### **1. Clone the Repository**

git clone https://github.com/Navabpranay/Handwrittedn-Digit-Recognition.git

cd Handwritten-Digit-Recognition

pip install tensorflow keras numpy matplotlib opencv-python

python test.py

---

## 📸 Sample Output

Here is an example prediction generated by the model:

1/1 ━━━━━━━━━━━━━━━━━━━━ 2s 2s/step

Predicted Digit: 2 with confidence 1.0000
---

## 📁 Project Folder Structure

Handwritten-digit-recognition/

├── model.py

├── train.py

├── test.py

├── images

├── README.md   

├── mnist_cnn.h5   

└── doc                       

---

## 👤 Author

**Nawab Pranay Goud**  
Deep Learning & AI Enthusiast  

GitHub: https://github.com/NawabPranay

LinkedIn: *linkedin.com/in/pranaygoudnawab*  

Feel free to ⭐ the repository if you found it useful!

---






